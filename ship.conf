# Values ship reads. The prose is in RELEASE.md; the loop is in
# ~/notes/releasing.md. Format and keys: anderix/ship, docs/PROFILE.md.
loop=hand-cut
version_file=Cargo.toml
version_mirrors=packaging/windows/AppxManifest.xml.in packaging/macos/Info.plist.in
apt_package=slipcase-desktop
deb_packages=1
deb_arches=amd64 arm64
crates=slipcase-desktop
homebrew=no
winget=
store_lanes=msix appstore
display_name=Slipcase Desktop
packages_workflow=linux.yml
